# 更新记录

本文记录 Cyrene Mobile 的重要变更。

## 0.1.1 — 2026-08-01

### 权限与运行

- 新增自动、默认与 Plan 三种权限模式；首次安装默认启用自动模式，并可在输入框
  或 App 偏好设置中切换。
- 对话与任务可直接显示桌面端提权或确认请求，支持快捷选项和自定义回答后继续运行。

### 项目与会话

- “最近会话”升级为“所有会话”，汇总全部授权项目的任务和对话，标注所属项目并
  按更新时间混排。
- 支持从跨项目会话列表安全打开、重命名和删除对应项目中的会话。
- 新对话空状态的“当前项目”胶囊可直接切换项目；任务项目选择器移入“创建任务”
  卡片，减少顶栏视觉负担。

### 模型与系统

- 可从移动端发起桌面 OpenAI OAuth 登录、退出登录，发现 Codex 模型并设置模型与
  reasoning effort；OAuth 令牌仍只保留在桌面端。
- 状态栏与导航栏采用透明沉浸式配色，并同步适配浅色和深色系统图标。

## 0.1.0 — 2026-07-31

Cyrene Mobile 的首个公开版本。该版本以 Cyrene Desktop 的交互与信息结构为
基准，为 Android 提供安全、完整且适合触控操作的远程工作台。

### 对话

- 对话页改为与桌面端一致的内容层级：用户消息右对齐，助手回复保持连续正文，
  时间与操作行不再占用独立卡片。
- 使用 Markwon 渲染 Markdown，支持标题、强调、列表、链接和代码等常用格式。
- 工具调用与执行过程使用语义化状态、进度和深浅色配色。
- 修正深色模式下用户消息与工具调用卡片对比度错误。
- 附件和图片可直接显示在消息中；点击图片进入查看器，并保留文件名和相关操作。
- 顶栏仅显示当前页面或已打开会话的真实标题，不再重复显示项目名、Cyrene
  副标题或第二行会话标题。
- 新会话默认显示“新对话”，发送第一条消息后按桌面端逻辑生成标题。
- 会话项支持长按打开操作菜单，可重命名或在二次确认后永久删除。

### 输入与任务

- 输入框改为无阴影的浮动输入框，悬浮在内容之上，不再使用页面分割线。
- 对话与任务详情共用同一套多行输入、附件、发送、停止和运行状态控件。
- 支持默认模式与 Plan 模式切换。
- 附件选择支持多文件、可移除预览和有界传输。
- 任务支持携带附件派发，并可暂停、恢复、取消；任务产物可查看和下载。
- 移除“Cyrene 是 AI，可能会犯错。请核实回复。”提示。

### 导航与会话管理

- 取消底部 Tab 栏，改为可手势打开的左侧菜单。
- 菜单上部显示设备、对话、任务与终端入口；设置移动为右下角圆形按钮。
- 移除“项目”入口。
- “最近对话”改为“最近会话”，按更新时间混合显示最近任务和对话。
- 对话列表合并重复的标题与创建区域；创建新对话移动到右下角按钮。
- 支持保存和切换多台已配对桌面设备。

### 桌面式右侧栏

- 在内容区向左滑动可连续拖出右侧栏；向右滑、点击遮罩或关闭按钮可收起。
- 概览页与桌面端对齐，显示运行摘要、输入/输出/合计 Token、会话信息，以及
  单条上下文占用进度。
- 上下文页显示总 Token、系统前缀、临时注入、用户/助手/工具消息拆分、
  Agent 收件箱、已使用工具包和会话统计。
- 子 Agent、变更、查看器、地图、计划、产物和分支等页签会在存在对应数据时
  自动出现，避免展示空页。
- 深色模式采用与桌面端一致的深海军蓝层级、卡片背景和语义色。

### 外观与系统集成

- 支持浅色、深色和跟随系统三种主题，启动阶段同步系统夜间模式，减少闪屏。
- 更新应用图标、圆形图标和 Android 12+ 启动画面。
- 优化中英文文案、触控尺寸、移动端字号与状态栏/安全区适配。
- 设置页新增 GitHub Release 更新检查、APK 下载进度和系统安装流程。

### 安全与连接

- 通过桌面 RemoteGateway 完成 Ed25519 签名和
  X25519/HKDF/ChaCha20-Poly1305 端到端加密。
- 限制连接到 Loopback、局域网、IPv6 本地地址与 Tailscale 地址。
- 桌面端按设备权限和项目范围校验远程命令；重命名与删除会再次验证会话归属。
- Identity 私钥继续由 Android Keystore 主密钥保护，App 数据不参与系统备份。

### 兼容性

- Android 9（API 28）或更新版本。
- 完整功能需要 Cyrene Desktop 0.7.0 beta9 或更新版本。较早桌面端可能缺少
  上下文详情、动态右侧栏数据、附件或会话重命名/删除命令。

### 验证

- Android 单元测试、Lint、Debug 编译和 APK 装配。
- Android API 35 模拟器的浅色/深色界面检查。
- 左右侧栏拖动、Markdown、附件、任务输入、图片查看和最近会话导航。
- 长按菜单、重命名、删除确认及删除后的列表刷新。
- 桌面端远程协议的会话读取、更新、删除、跨项目拒绝和附件传输。
